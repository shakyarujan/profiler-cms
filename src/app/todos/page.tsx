import TodosList from "@/app/todos/TodosList";
import React from "react";

function Todos() {
  // return <TodosList />;
  return (
    <div>
      <h1>There is where the todo page render.....</h1>
    </div>
  );
}

export default Todos;
